// LEC-2
// As a user, I need to see all the schools represented as a marker

// For this file, uncomment and then run: npx playwright test --headed --debug and step over

//import { test, expect } from "@playwright/test";

// test("test", async ({ page }) => {
//   await page.goto("http://localhost:3000/dashboard-insights");

//   await page.waitForSelector("canvas");

//   await page.waitForTimeout(2000);

//   await page.getByRole("region", { name: "Map" }).click({
//     position: { x: 114, y: 435 },
//   });

//   await page.getByRole("button", { name: /clear selected school/i }).click();
// });
